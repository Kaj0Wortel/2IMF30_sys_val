forall c1: Nat . val(c1 < 4) =>
forall c2: Nat . val(c2 < 4) =>
forall r: Rail . val(id(r) <= 8) =>
forall r': Rail . val(id(r) <- 8) =>
	[true* . cartAtRail(c1, r) . !cartAtRail(c1, r')* . cartAtRail(c2, r)] false